# Quality Gates

- VERDADE ABSOLUTA: testes unitarios devem manter 100% de cobertura global (statements, branches, functions e lines).
- rodar `npm run lint`
- rodar `npm run test`
- rodar `npm run build`
